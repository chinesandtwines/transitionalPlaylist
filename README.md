transitionalPlaylist
====================

Reads a .m3u playlist and uses the contained file locations to analyze the .mp3 files. 
Currently only waveform amplitude is analyzed. Other methods are are on the way.
